<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="HBProducts.Views.AboutPage"
             xmlns:vm="clr-namespace:HBProducts.ViewModels"
             Title="{Binding Title}">

    <ScrollView>
        <StackLayout
            VerticalOptions="CenterAndExpand"
            HorizontalOptions="CenterAndExpand"
            Margin="7,7,7,7">
            <Editor Text="{Binding AboutCompany}"
                    IsReadOnly="True"
                    FontSize="Body"/>

            <Label Text="{Binding Paragraph1Label}" TextColor="Black" FontSize="Medium" 
                   FontAttributes="Bold" Margin="0,15,0,0"/>
            <Editor Text="{Binding Paragraph1Text}"
                    IsReadOnly="True"
                    FontSize="Body"/>

            <Label Text="{Binding Paragraph2Label}" TextColor="Black" FontSize="Medium" 
                   FontAttributes="Bold" Margin="0,15,0,0"/>
            <Editor Text="{Binding Paragraph2Text}"
                    IsReadOnly="True"
                    FontSize="Body"/>



        </StackLayout>
    </ScrollView>

    
</ContentPage>
